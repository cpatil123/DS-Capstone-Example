### AUTHOR : YANAL KASHOU ###

################
### Global.R ### 
################

# The Magical Sampling (The only real sampling is through the function sample, forget rbinom, rgeom, retc..)
# sampledRaw <- Raw[sample(1:length(Raw), .5*(length(Raw)), replace=FALSE)] 
# saveRDS(sampledRaw, "SRaw.Rds")